--- a/src/base.rs
+++ b/src/base.rs
@@ -18,7 +18,6 @@ use matrix_sdk::{
 };
 
 use ratatui::{buffer::Buffer, layout::Rect};
-use ratatui_image::picker::{Picker, ProtocolType};
 
 use modalkit::{
     actions::{
@@ -91,7 +90,6 @@ use modalkit::{
 };
 
 use crate::config::ImagePreviewProtocolValues;
-use crate::message::ImageStatus;
 use crate::preview::{source_from_event, spawn_insert_preview};
 use crate::{
     message::{Message, MessageEvent, MessageKey, MessageTimeStamp, Messages},
@@ -1468,7 +1466,6 @@ pub struct ChatStore {
     pub downloads: DownloadTracker,
     pub typing: TypingStore,
     pub settings: ApplicationSettings,
-    pub image_preview: Picker,
     pub preview_mgr: PreviewManager,
     pub need_load: HashSet<OwnedRoomId>,
     pub sync_info: SyncInfo,
@@ -1480,16 +1477,6 @@ pub struct ChatStore {
 impl ChatStore {
     pub fn new(settings: ApplicationSettings) -> Self {
         let collator = feruca::Collator::default();
-        let image_preview = match settings.tunables.image_preview.protocol {
-            ImagePreviewProtocolValues::Auto => Picker::from_query_stdio(),
-            ImagePreviewProtocolValues::Halfblocks => {
-                Picker::from_fontsize((8, 16)).with_protocol_type(ProtocolType::Halfblocks)
-            },
-            ImagePreviewProtocolValues::Kitty => {
-                Picker::from_fontsize((8, 16)).with_protocol_type(ProtocolType::Kitty)
-            },
-            ImagePreviewProtocolValues::Sixels => Picker::from_fontsize((8, 16)),
-        };
         let preview_mgr = PreviewManager::new();
 
         ChatStore {
@@ -1500,7 +1487,6 @@ impl ChatStore {
             downloads: DownloadTracker::default(),
             typing: TypingStore::default(),
             settings,
-            image_preview,
             preview_mgr,
             need_load: HashSet::new(),
             sync_info: SyncInfo::default(),
--- a/src/message.rs
+++ b/src/message.rs
@@ -1,7 +1,6 @@
 //! Matrix message storage and rendering
 use std::cmp::Ordering;
 
-use ratatui_image::protocol::StatefulProtocol;
 use unicode_width::UnicodeWidthStr;
 
 use matrix_sdk::{
@@ -46,12 +45,6 @@ pub type MessageKey = (OwnedEventId, Option<OwnedEventId>);
 #[derive(Clone, Debug)]
 pub struct MessageTimeStamp(pub MilliSecondsSinceUnixEpoch);
 
-#[derive(Debug)]
-pub enum ImageStatus {
-    Loading,
-    Loaded(StatefulProtocol),
-}
-
 /// A message to be shown in the message scrollback.
 #[derive(Debug)]
 pub struct Message {
--- a/src/preview.rs
+++ b/src/preview.rs
@@ -8,7 +8,6 @@ use matrix_sdk::{
 };
 
 use ratatui::{buffer::Buffer, layout::Rect, style::Style, text::Text, widgets::Paragraph};
-use ratatui_image::{protocol::StatefulProtocol, Resize};
 
 use modalkit_ratatui::textbox::TextBoxState;
 
@@ -18,9 +17,7 @@ use crate::{
         RoomInfo,
     },
     config::ApplicationSettings,
-    message::ImageStatus,
 };
-use ratatui_image::picker::Picker;
 
 #[derive(Clone, Debug, Eq, PartialEq)]
 pub enum PreviewType {
@@ -28,7 +25,6 @@ pub enum PreviewType {
         width: u32,
         height: u32,
     },
-    Image(ImageStatus),
 }
 
 #[derive(Clone, Debug, Eq, PartialEq)]
@@ -62,25 +58,6 @@ impl PreviewManager {
         }
     }
 
-    pub fn get_image(&mut self, preview: &Preview) -> &mut StatefulProtocol {
-        match &preview.ptype {
-            PreviewType::Image(status) => match status {
-                ImageStatus::Loaded(protocol) => {
-                    if let Some(cached) = self.cache.get_mut(&preview.url) {
-                        cached
-                    } else {
-                        // This shouldn't happen, but handle it gracefully
-                        panic!("Image not found in cache");
-                    }
-                },
-                ImageStatus::Loading => {
-                    panic!("Image still loading");
-                },
-            },
-            _ => panic!("Not an image preview"),
-        }
-    }
-
     pub fn render_text_preview(
         &self,
         preview: &Preview,
@@ -117,19 +94,6 @@ impl PreviewManager {
                 (1, 1)
             },
 
-            PreviewType::Image(status) => match status {
-                ImageStatus::Loading => (1, 1),
-                ImageStatus::Loaded(protocol) => {
-                    let dyn_img = protocol.background_color(None);
-                    let resized = Resize::Fit(Some((area.width as u32, area.height as u32)));
-                    let rendered = dyn_img.resize(resized);
-                    (
-                        (rendered.width() / protocol.font_size().0 as u32).max(1) as u16,
-                        (rendered.height() / protocol.font_size().1 as u32).max(1) as u16,
-                    )
-                },
-            },
-
             PreviewType::Text { width, height } => (
                 (*width as u16).min(area.width),
                 (*height as u16).min(area.height),
@@ -174,7 +138,6 @@ pub async fn spawn_insert_preview(
     mut room: matrix_sdk::Room,
     settings: ApplicationSettings,
     store: AsyncProgramStore,
-    image_picker: Picker,
 ) {
     tokio::spawn(async move {
         if let Some(event) = source_from_event(&event, &mut room).await {
@@ -187,30 +150,6 @@ pub async fn spawn_insert_preview(
                 "text/plain" => {
                     insert_text_preview(event, store).await;
                 },
-                t if t.starts_with("image/") => {
-                    insert_image_preview(event, store, image_picker).await;
-                },
-                _ => return,
-            }
-        }
-    });
-}
-
-async fn insert_image_preview(
-    event: SourcedPreviewEvent,
-    store: AsyncProgramStore,
-    mut picker: Picker,
-) {
-    if let Ok(dyn_img) = image::load_from_memory(&event.data) {
-        let protocol = picker.new_resize_protocol(dyn_img);
-        let preview = Preview {
-            event_id: event.event_id.clone(),
-            url: event.url.clone(),
-            ptype: PreviewType::Image(ImageStatus::Loaded(protocol)),
-        };
-
-        if let Ok(mut guard) = store.try_lock() {
-            guard.insert_preview(event.room_id, preview);
+                _ => return,
         }
     });
--- a/src/windows/room/scrollback.rs
+++ b/src/windows/room/scrollback.rs
@@ -1397,12 +1397,6 @@ impl<'a> MessageBodyWidget<'a> {
                 let (width, height) =
                     self.preview_mgr.get_preview_dimensions(preview, area);
 
-                if matches!(preview.ptype, PreviewType::Image(_)) {
-                    let image_widget = self.preview_mgr.get_image(preview);
-                    let rect = Rect::new(area.x, area.y + y, area.width, height);
-
-                    Widget::render(image_widget, rect, buf);
-                    y += height;
                 }
                 PreviewType::Text(preview) => {
                     self.preview_mgr.render_text_preview(